const sessionKey='sias_session';let role='guru';const $=s=>document.querySelector(s);document.querySelectorAll('.role-tab').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.role-tab').forEach(x=>x.classList.remove('active'));b.classList.add('active');role=b.dataset.role;$('#loginUser').value='';$('#loginPass').value='';$('#loginError').textContent='';$('#loginUser').focus()}));function login(){const u=$('#loginUser').value.trim(),p=$('#loginPass').value;if(role==='guru'){if(u.toLowerCase()!=='guru'||p!=='guru123')return $('#loginError').textContent='Username/password guru salah.';sessionStorage.setItem(sessionKey,'guru');sessionStorage.removeItem('sias_student_nis');sessionStorage.removeItem('sias_student_name');location.href='dashboard-guru.html';return}if(u.toLowerCase()!=='siswa')return $('#loginError').textContent='Username siswa harus: siswa';const students=JSON.parse(localStorage.getItem('siswa_v1')||'[]');const student=students.find(x=>String(x.nis).toLowerCase()===p.trim().toLowerCase());if(!student)return $('#loginError').textContent='Password/NIS siswa tidak ditemukan. Pastikan NIS sudah terdaftar.';sessionStorage.setItem(sessionKey,'siswa');sessionStorage.setItem('sias_student_nis',String(student.nis));sessionStorage.setItem('sias_student_name',String(student.name||''));location.href='dashboard-siswa.html'}$('#loginBtn').addEventListener('click',login);$('#loginPass').addEventListener('keydown',e=>{if(e.key==='Enter')login()});$('#loginUser').addEventListener('keydown',e=>{if(e.key==='Enter')$('#loginPass').focus()});$('#togglePass').addEventListener('click',()=>{const i=$('#loginPass'),b=$('#togglePass');i.type=i.type==='password'?'text':'password';b.textContent=i.type==='password'?'👁️':'🙈'});sessionStorage.removeItem(sessionKey);sessionStorage.removeItem('sias_student_nis');sessionStorage.removeItem('sias_student_name');
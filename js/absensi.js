if(role!=='guru')location.replace('dashboard-siswa.html');const key='absensi_siswa_v1',studentKey='siswa_v1';let data=JSON.parse(localStorage.getItem(key)||'[]'),students=JSON.parse(localStorage.getItem(studentKey)||'[]');const date=$('#date'),student=$('#student'),className=$('#className'),schoolName=$('#schoolName'),teacherName=$('#teacherName');date.value=new Date().toISOString().slice(0,10);schoolName.value=localStorage.getItem('school_name')||'';teacherName.value=localStorage.getItem('teacher_name')||'';const classes=[...new Set(students.map(x=>x.className).filter(Boolean))];$('#classOptions').innerHTML=classes.map(x=>`<option value="${x}"></option>`).join('');$('#studentOptions').innerHTML=students.map(x=>`<option value="${x.name}"></option>`).join('');student.addEventListener('input',()=>{const s=students.find(x=>x.name.toLowerCase()===student.value.trim().toLowerCase());if(s)className.value=s.className});$('#add').addEventListener('click',()=>{if(!date.value||!student.value.trim()||!className.value.trim())return alert('Isi tanggal, nama siswa, dan kelas terlebih dahulu.');data.push({student:student.value.trim(),className:className.value.trim(),date:date.value,status:$('#status').value});localStorage.setItem(key,JSON.stringify(data));localStorage.setItem('school_name',schoolName.value.trim());localStorage.setItem('teacher_name',teacherName.value.trim());alert('Absensi berhasil disimpan.');student.value='';location.href='dashboard-guru.html'});
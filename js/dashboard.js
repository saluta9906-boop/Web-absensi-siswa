const data=JSON.parse(localStorage.getItem('absensi_siswa_v1')||'[]');const c={Hadir:0,Izin:0,Sakit:0,Alpa:0};data.forEach(x=>c[x.status]++);const total=data.length;$('#totalStat').textContent=total;$('#hadirStat').textContent=c.Hadir;$('#izinStat').textContent=c.Izin;$('#sakitStat').textContent=c.Sakit;$('#alpaStat').textContent=c.Alpa;const pct=total?Math.round(c.Hadir/total*100):0;$('#percentStat').textContent=pct+'%';$('#progressBar').style.width=pct+'%';const max=Math.max(1,...Object.values(c));$('#chart').innerHTML=Object.entries(c).map(([k,v])=>`<div class="bar-wrap"><span class="bar-value">${v}</span><div class="bar" style="height:${Math.max(3,v/max*130)}px"></div><span class="bar-label">${k}</span></div>`).join('');const esc=s=>String(s).replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[m]));const recent=[...data].slice(-5).reverse();$('#recent').innerHTML=recent.length?recent.map(x=>`<div class="recent-item"><div><div class="recent-name">${esc(x.student)}</div><div class="recent-meta">${esc(x.className)} · ${x.date}</div></div><span class="pill pill-${x.status.toLowerCase()}">${x.status}</span></div>`).join(''):'<p class="empty">Belum ada data absensi.</p>';$('#todayLabel').textContent=new Date().toLocaleDateString('id-ID',{weekday:'long',day:'numeric',month:'long',year:'numeric'});